import { Placeholder } from '@/features/common/Placeholder';

export default function Trade() {
  return (
    <Placeholder
      icon="pulse"
      title="Trade"
      subtitle="The live chart + order ticket sandbox lands here (M4)."
    />
  );
}
